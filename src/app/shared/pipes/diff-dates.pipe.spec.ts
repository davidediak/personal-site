import {DiffDatesPipe} from './diff-dates.pipe';

describe('DiffDatesPipe', () => {
    it('create an instance', () => {
        const pipe = new DiffDatesPipe();
        expect(pipe).toBeTruthy();
    });
});
